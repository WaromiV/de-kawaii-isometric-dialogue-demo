# Security Notes

- Do not commit API keys or tokens.
- Configure sensitive values via environment variables (for this project: `ELEVENLABS_API_KEY`).
- `.env` is ignored by git; use `.env.example` as the template.
